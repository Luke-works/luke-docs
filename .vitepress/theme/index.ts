import DefaultTheme from 'vitepress/theme'
import './custom.css'

// We keep VitePress's default 3-column theme (nav sidebar / content / on-this-page
// TOC — the same shape as the Camunda 7 manual) and only re-skin it via custom.css.
export default DefaultTheme
